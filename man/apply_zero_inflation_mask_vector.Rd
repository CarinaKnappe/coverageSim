% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ZIGDM.R
\name{apply_zero_inflation_mask_vector}
\alias{apply_zero_inflation_mask_vector}
\title{Zero-inflation masking for a single alpha/weight vector}
\usage{
apply_zero_inflation_mask_vector(alpha, zero_inflation, pad_value = 1e-24)
}
\arguments{
\item{alpha}{numeric vector of Dirichlet/GD weights for a single gene.}

\item{zero_inflation}{numeric in [0, 1), exclusion probability.}

\item{pad_value}{numeric, positions at or below this are already
structurally absent (e.g. sim_sequence_bias() maps zero to 1e-24) and are
left untouched, matching apply_zero_inflation_mask()'s convention.}
}
\value{
alpha, with a random subset of eligible entries set to pad_value;
at least one eligible entry is always kept.
}
\description{
Vector counterpart of \code{apply_zero_inflation_mask()}, used by
\code{draw_site_probabilities()} (the deferred, simulated-RPF
fragment-geometry sampling path), which works with one per-gene weight
vector at a time rather than a padded n x K matrix. Delegates to the
matrix version via a one-row matrix so both call sites share the same
exclusion/guard logic.
}
\keyword{internal}
