# CLAUDE.md

Project instructions live in **`AGENTS.md`** at the repo root; read it first. Personal notes and conventions are in the git-ignored `.agents/` folder, if present. For current status check `git status` / `git log`.
